let { test } = import("test");
let { assertEqual } = import("assertion");

let add = (a, b) -> a + b;
let sub = (a, b) -> a - b;
let one = 1;

test("add", -> assertEqual(add(one, 2), 3));

test("sub", -> assertEqual(sub(one, 2), -1));

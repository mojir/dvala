let { test } = import("test");
let { assertEqual } = import("assertion");
let { greet, add } = import("../main");

test("greet returns greeting", -> assertEqual(greet("World"), "Hello, World!"));

test("add sums two numbers", -> assertEqual(add(2, 3), 5));

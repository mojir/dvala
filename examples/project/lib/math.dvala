let clamp = (value, lo, hi) -> min(max(value, lo), hi);

let lerp = (a, b, t) -> do
  let diff = b - a;
  a + diff * t;
end;

let inRange = (value, lo, hi) -> value >= lo && value <= hi;

let isEven = (n) ->
  n % 2 == 0;

{
  clamp: clamp,
  lerp: lerp,
  inRange: inRange
}
